Steps to follow

1. Get the pull of the code in your local machine.
2. In the root directory, run "npm install".
3. Run command "npm run start" to use the project locally.

Note: You must have Node.js installed in your system
